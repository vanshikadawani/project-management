# Frontend Environment Configuration
# Optional: URL of the backend API / Socket.IO server (if hosted on a separate domain).
# Leave empty for same-domain deployment, Vercel Services routing, or local Vite dev proxy.
VITE_API_URL=""
